Message Format
COMMAND, ID, WIDGET_TYPE, 
NUM1, NUM2, NUM3, NUM4, NUM5, 
BOOL1, BOOL2, BOOL3, BOOL4, BOOL5, 
STR1, STR2, STR3, STR4, STR5

Commands
CREATE
DELETE
SET_VISIBLE
SET_ENABLED
SET_TEXT
SET_POSITION
SET_STYLE

Button
CREATE
	COMMAND: CREATE,
	ID: INT,
	WIDGET_TYPE: BUTTON,
	STR1: STRING (text to show in button.)


WINDOW
  - Anchor Points

VStack
  - Is size of contents
  - Can specify spacing 
  - Can specify alignment

